# Test fixtures

Reduced projects that `tests/e2e/*.test.sh` and `tests/lint.sh` start and
validate. They are not the demo repositories: `portta-demo-*` live outside this
repository, under `PORTTA_PROJECTS_HOME` (docs/development/adr/0044-example-projects-live-in-projects-home.md).
